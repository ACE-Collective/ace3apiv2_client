# configuration for openapi-python-client (https://github.com/openapi-generators/openapi-python-client)
# controls how the aceapi_v2_client package is generated from the vendored openapi.json
# see scripts/regenerate.sh for how this is invoked
package_name_override: aceapi_v2_client
project_name_override: aceapi-v2-client
# keep generated package metadata in sync with the hand-authored pyproject.toml
package_version_override: 2.4.0

# the api serves two endpoints (/alerts/{uuid}/download and /crashes/{id}/download)
# as application/zip. the generator only understands application/json,
# application/octet-stream and text/*; anything else makes it drop the success
# response entirely, leaving sync() returning None on a perfectly good download.
# mapping zip onto octet-stream makes those operations return the raw bytes.
content_type_overrides:
  application/zip: application/octet-stream
